version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

# 用 Sphinx 构建；指向 docs/conf.py
sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .                  # 安装你的包（支持 src 布局）
    - requirements: docs/requirements.txt
